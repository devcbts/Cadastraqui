export default function Term({ text }) {
    return (
        <div style={{ padding: '12px ', width: '100%', textAlign: 'center', backgroundColor: "#CFCFCF", color: "#1F4B73", fontSize: '12px', borderRadius: '8px' }}>
            {text}
        </div>
    )
}